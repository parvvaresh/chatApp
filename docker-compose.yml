version: "3.9"

services:
  chat-server:
    build: .
    container_name: chat-server
    ports:
      - "8080:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./chat.db:/app/chat.db
    restart: unless-stopped
    environment:
      - PORT=8080
    command: ./chat-server
